"use strict";import isAbsoluteURL from"../helpers/isAbsoluteURL.js";import combineURLs from"../helpers/combineURLs.js";export default function buildFullPath(baseURL,requestedURL){if(baseURL&&!isAbsoluteURL(requestedURL)){return combineURLs(baseURL,requestedURL)}return requestedURL}